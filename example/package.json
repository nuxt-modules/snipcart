{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "start": "nuxt start",
    "run": "cypress run",
    "open": "cypress open",
    "cypress:run": "yarn build && start-server-and-test start http://localhost:3000 run",
    "cypress:dev": "start-server-and-test dev http://localhost:3000 open"
  }
}
